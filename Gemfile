source "https://rubygems.org"

# GitHub Pages builds this site with its own pinned Jekyll version; this Gemfile
# is for local previews only (`bundle exec jekyll serve`).
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-sitemap"
end
